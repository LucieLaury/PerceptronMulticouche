
mlp = nouveau mlp (couches, taux d'apprentissage, fonction)   // Crée le MLP avec ces trois paramètres
input = valeurs d'entrée        // définit des valeurs de départ (ici, deux doubles qui sont soit 1.0 ou -1.0)

pour nb_itérations :
    taux_erreur = mlp.backPropagate(input, sortie_souhaitée) // la sortie sohaitée est un tableau de double contenant un -1.0 ou 1.0
                                                             // on veut garder ici le taux d'erreur le plus petit
                                                             // pour voir si le programme apprend bien



